<div class="long-url-form">
  <%= form_for :url, url: urls_path do |f| %>
    <%= f.label "Paste your long url here" %>
    <%= f.text_field :long %>
    <%= f.submit "Shorten URL" %>
  <% end %>
</div><hr>
Sort By: <%= link_to "Created At", "#", id: "show-created-at", class: "colorize"  %>
<%= link_to "Most Popular", "#", id: "show-popular" %>
<div class="created-when" id="created-at">
  <% if @urls_time.present? %>
    <table>
      <tr>
        <th><h4>Long URL</h4></th>
        <th></th>
        <th><h4>Shortened URL</h4></th>
        <th></th>
        <th>Created</th>
        <th></th>
        <th>Clicks</th>
      </tr>
      <% @urls_time.each do |url| %>
        <tr>
          <td>
            <%= url.long %>
          </td>
          <td>
          </td>
          <td>
             <%= link_to url.short, url_path(url) %>
          </td>
          <td>
          </td>
          <td>
            <%= distance_of_time_in_words(Time.now, url.created_at) %> ago
          </td>
          <td>
          </td>
          <td>
            <%= url.count %>
          </td>
        </tr>
      <% end %>
    </table>
  <% end %>
</div>
<div class="popular hidden" id="popular">
  <% if @urls_popular %>
    <table>
      <tr>
        <th><h4>Long URL</h4></th>
        <th></th>
        <th><h4>Shortened URL</h4></th>
        <th></th>
        <th>Created</th>
        <th></th>
        <th>Clicks</th>
      </tr>
      <% @urls_popular.each do |url| %>
        <tr>
          <td>
            <%= url.long %>
          </td>
          <td>
          </td>
          <td>
             <%= link_to url.short, url_path(url) %>
          </td>
          <td>
          </td>
          <td>
            <%= distance_of_time_in_words(Time.now, url.created_at) %> ago
          </td>
          <td>
          </td>
          <td>
            <%= url.count %>
          </td>
        </tr>
      <% end %>
    </table>
  <% end %>
</div>
